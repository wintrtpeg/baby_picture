# design.md — 디자인 시스템

아기 사진·문구 기록 앱의 디자인 규칙 문서. **Flutter 코드는 이 문서의 토큰을 따른다.**
화면 목업: `design/` 폴더의 `.dc.html` 파일들.

---

## 1. 원칙

세 줄로 요약되는 디자인 결정. 새 화면을 만들 때 이 셋과 충돌하면 그 화면이 틀린 것이다.

1. **색은 사진에서만 나온다.** UI는 거의 무채색이다. 아기 사진이 화면에서 유일하게
   채도 높은 요소여야 한다. 강조색을 장식으로 쓰지 않는다.
2. **문구(caption)는 인쇄될 원고다.** 문구를 다루는 모든 UI는 "이 글이 책에 들어간다"는
   사실이 보이게 만든다. 흘러가는 SNS 텍스트처럼 보이면 안 된다.
3. **구조로 나눈다, 그림자로 나누지 않는다.** 카드는 1px 헤어라인으로 구분한다.
   떠 있는 그림자는 FAB 하나뿐이다.

---

## 2. 색상

토큰 이름으로만 참조한다. 화면에 하드코딩된 hex를 새로 만들지 않는다.

### 표면 / 텍스트

| 토큰 | 값 | 용도 | 대비 |
|---|---|---|---|
| `bg` | `#F7F6F4` | 화면 배경 (따뜻한 오프화이트) | — |
| `surface` | `#FFFFFF` | 카드, 입력 필드 | — |
| `sunken` | `#EFEEEB` | 세그먼트 컨트롤 트랙, 칩 배경 | — |
| `ink` | `#1A1A18` | 제목, 본문, 주요 버튼 배경 | 15.2:1 |
| `ink2` | `#55534E` | 보조 본문, 부차 버튼 글자 | 7.1:1 |
| `ink3` | `#6B6963` | 라벨, 캡션, 비활성 탭 글자 | 5.1:1 |
| `hairline` | `#E8E6E1` | 카드 테두리, 구분선 | — |
| `lineStrong` | `#D5D3CD` | 점선 테두리, 빈 상태 | — |
| `mute` | `#B8B6B0` | **텍스트 금지.** 아이콘·구분점·플레이스홀더 링 전용 | 1.9:1 |

### 기능색

| 토큰 | 값 | 용도 | 대비 |
|---|---|---|---|
| `accent` | `#2E6B58` | 문구 진행/완료 상태. **그 외 사용 금지** | 5.8:1 (bg) |
| `accentTint` | `#E7EFEB` | 자동 인식 배지 배경 | — |
| `warnBg` | `#FBF4EC` | 경고 카드 배경 | — |
| `warnLine` | `#F0E3D2` | 경고 카드 테두리 | — |
| `warnInk` | `#7A6140` | 경고 카드 보조 텍스트 | 5.3:1 |
| `warnIcon` | `#B07A4A` | 경고 아이콘 **(아이콘 전용)** | 3.4:1 |
| `onDark` | `#FFFFFF` | 잉크 배경 위 글자 | 15.2:1 |

**대비 규칙**: 모든 텍스트는 배경 대비 4.5:1 이상. `mute`는 이 기준을 통과하지 못하므로
텍스트에 쓰지 않는다. 아이콘·테두리 등 비텍스트 요소는 3:1 이상.

주요 버튼은 `accent`가 아니라 `ink`다. 초록 버튼보다 잉크 블랙이 절제되어 보이고,
`accent`를 문구 상태 전용으로 남겨두면 "글이 얼마나 채워졌는지"가 화면에서 유일하게
색을 가진 정보가 된다.

---

## 3. 타이포그래피

### 서체

| 용도 | 서체 | 비고 |
|---|---|---|
| 한글 전부 | **Pretendard** (300 / 400 / 500 / 600) | 앱에 번들 |
| 라틴 숫자 · 짧은 라벨 | **Instrument Serif** (400) | `D+561`, `218`, `3/12` 등 |

> 목업은 IBM Plex Sans KR로 그려져 있다. 캔버스가 Google Fonts만 불러올 수 있기 때문이다.
> 실제 앱은 **Pretendard**를 번들해 쓴다 — 한글 힌팅과 굵기 단계가 더 낫고 국내 앱 표준에 가깝다.
> 두 서체의 메트릭이 비슷해서 그대로 교체하면 된다.

세리프는 **독립된 라틴 숫자에만** 쓴다. 한글과 한 문장에 섞지 않는다
(`D+561` ⭕ / `생후 561일` ❌ — 후자는 전부 Pretendard).

### 스케일

| 토큰 | 크기 | 굵기 | 행간 | 자간 | 용도 |
|---|---|---|---|---|---|
| `displayNum` | 54 | Serif 400 | 0.92 | -1.0 | 타임라인 D+일수 |
| `statNum` | 30 | Serif 400 | 1.0 | 0 | 통계 숫자 |
| `title1` | 24 | 600 | 1.35 | -0.8 | 화면 제목 |
| `title2` | 22 | 600 | 1.3 | -0.7 | 모먼트 제목 |
| `title3` | 16.5 | 600 | 1.35 | -0.4 | 카드 제목 |
| `navTitle` | 15 | 600 | 1.3 | -0.4 | 상단 바 |
| `field` | 16 | 500 | 1.3 | -0.3 | 입력 값 |
| `body` | 14.5 | 300 | 1.7 | 0 | **문구 본문** |
| `body2` | 13 | 300 | 1.6 | 0 | 카드 안 본문 |
| `button` | 15 | 500 | 1.0 | -0.3 | 버튼 |
| `label` | 11.5 | 400 | 1.4 | -0.1 | 필드 라벨, 캡션 |
| `tab` | 11 | 400 / 600 | 1.0 | -0.1 | 탭 바 (활성 600) |

**한글 최소 크기는 11px.** 한글 음절은 같은 크기의 라틴보다 획이 많아 더 빨리 뭉갠다.
10px 이하를 쓰지 않는다.

문구 본문(`body`)만 굵기 300에 행간 1.7이다. 긴 글을 편하게 읽히게 하고,
UI 텍스트와 시각적으로 구분해 "이건 원고"라는 신호를 준다.

---

## 4. 간격 · 형태

- **그리드**: 4px 배수
- **좌우 여백**: 20px 고정 (모든 화면)
- **하단 안전 영역**: 34px (홈 인디케이터). 탭 바·주요 버튼 하단 패딩에 반드시 포함
- **반경**: 카드 16~18 / 큰 사진 16 / 중간 사진 14 / 작은 사진 10~12 / 버튼 15 /
  작은 버튼 13 / 칩 7 / 원형 999
- **높이**: 주요 버튼 54 · 부차 버튼 46 · 필드 최소 60 · 탭 항목 최소 44 · FAB 56
- **그림자**: FAB만 `0 8px 24px rgba(26,26,24,0.22)`. 카드는 그림자 없이 1px 헤어라인
- **모션**: 240ms `cubic-bezier(0.22, 1, 0.36, 1)`

레이아웃은 flex/grid + `gap`으로 짠다. 형제 요소를 각자의 margin으로 띄우지 않는다.

---

## 5. 컴포넌트 규칙

### 카드
`surface` 배경 + 1px `hairline` 테두리 + 반경 16~18. 그림자 없음.
사진을 품는 카드는 안쪽에 5px 패딩을 두고 사진에 별도 반경을 준다.

### 사진 그리드 (모먼트 카드)
`grid-template-columns: 2fr 1fr` + 2행. 첫 사진이 2행을 차지하고 나머지 2장이 오른쪽에.
사진이 3장을 넘으면 마지막 칸에 `rgba(26,26,24,0.38)` 오버레이와 `+N`(세리프).

### 문구 표시
- **작성됨**: `body` 굵기 300, 아래에 작성자 칩 (16px 아바타 + `label` "엄마가 씀")
- **비어 있음**: 1px 점선 `lineStrong`, 반경 12, 높이 46, 가운데 `+ 문구 추가`
- **진행 표시**: 3px 트랙 + `accent` 채움 + `문구 2 / 6` (`accent`, 11px)

비어 있는 문구를 회색 플레이스홀더 텍스트로 때우지 않는다. 점선 빈 칸으로 두어
"채워야 할 자리"임을 드러낸다.

### 입력 필드
`surface` + `hairline` 테두리 + 반경 14. 위에 `label`, 아래에 `field` 값.
**포커스**: 테두리를 1.5px `ink`로. 색으로 표시하지 않는다.

### 버튼
| 종류 | 배경 | 글자 | 테두리 | 높이 |
|---|---|---|---|---|
| 주요 | `ink` | `onDark` | 없음 | 54 |
| 부차 | `surface` | `ink2` | 1px `hairline` | 54 |
| 인라인 | 없음 | `ink` | 아래 1px 밑줄 | — |

한 화면에 주요 버튼은 하나뿐이다.

### 탭 바
상단 1px `hairline`, 배경 `rgba(247,246,244,0.96)`, 하단 패딩 34px.
활성 = `ink` + 굵기 600, 비활성 = 글자 `ink3` / 아이콘 `mute`.

### 아이콘
인라인 SVG, 선 스타일, `stroke-width: 1.6~1.8`, 둥근 끝.
크기는 14 / 17 / 21 / 24 중에서만. **이모지를 아이콘으로 쓰지 않는다.**

### 빈 상태 / 자리표시자
사진 자리는 회색 박스가 아니라 부드러운 톤의 그라데이션으로 둔다.
회색 박스는 "깨진 이미지"로 읽힌다.

---

## 6. 사진 처리 (인쇄 화질)

디자인이 아니라 저장 규칙이지만, 앨범 인쇄 품질을 좌우하므로 여기 둔다.

| 항목 | 값 |
|---|---|
| 저장 해상도 | 긴 변 **3200px** |
| 포맷 / 품질 | JPEG, 품질 **92** |
| 컬러 프로파일 | **sRGB 변환** (iPhone의 Display P3를 그대로 두면 인쇄 색이 죽는다) |
| 썸네일 | 긴 변 400px, 별도 저장 |
| 업스케일 | **금지.** 원본이 3200px보다 작으면 원본 그대로 |
| EXIF | 촬영일(`DateTimeOriginal`)과 회전(`Orientation`) **보존** |
| 크롭 | 원본을 자르지 않고 좌표만 저장 |

3200px 기준으로 300dpi에서 8×10인치까지, A4 전면은 약 270dpi로 인쇄 가능하다.
장당 약 2~3MB이며, 하루 3장씩 1년이면 약 2.5GB다.

---

## 7. 접근성

- 텍스트 대비 **4.5:1** 이상, 아이콘·테두리 **3:1** 이상
- 터치 영역 **최소 44×44**. 시각적으로 작은 요소는 투명 패딩으로 키운다
- 한글 최소 **11px**
- 색만으로 상태를 표현하지 않는다 (문구 진행은 색 + 숫자 `2 / 6`를 함께 표시)
- 하단 34px 안전 영역 확보

---

## 8. 화면 목록

`design/` 폴더:

| 파일 | 화면 |
|---|---|
| `Onboarding.dc.html` | 아기 등록 |
| `Main.dc.html` | 타임라인 (홈) |
| `MomentDetail.dc.html` | 모먼트 상세 — 사진별 문구 |
| `CaptionFlow.dc.html` | 문구 채우기 |
| `Upload.dc.html` | 업로드 / 새 모먼트 |
| `AlbumExport.dc.html` | 앨범 내보내기 |
| `Family.dc.html` | 가족 초대 |
| `DirectionA / B / C.dc.html` | 초기 방향 3안 (보관용) |

목업의 사진은 전부 자리표시자 그라데이션이다.

---

## 9. Flutter 적용

- 토큰은 `ThemeExtension`으로 정의하고 위젯에서 하드코딩 hex를 쓰지 않는다
- Pretendard를 `assets/fonts/`에 번들하고 `fontFamily`로 지정.
  Instrument Serif는 숫자 전용 `TextStyle`로 따로 둔다
- 자간은 `letterSpacing`(논리 픽셀)에 그대로 대응한다
- 하단 안전 영역은 `MediaQuery.viewPadding.bottom`과 34px 중 큰 값을 쓴다
- 사진 리사이즈는 EXIF 보존 옵션을 **명시적으로 켠다**. 기본값은 대개 EXIF를 지운다
